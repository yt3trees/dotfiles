## Gemini Added Memories
- Use a hyphen (-) followed by a single-byte space for bullet points. (e.g., - xxx)
- Use half-width parentheses () unless otherwise specified.
- When outputting Markdown files, do not use bold (****) or italic (**) syntax.
- When creating SQL queries that write to the database, such as UPDATE or DELETE, always use the `begin tran` format.
